{
  "name": "mihomo-manager-webui",
  "version": "0.4.0",
  "private": false,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && bash -n scripts/update-mihomo-subscription",
    "test": "node scripts/smoke-test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
