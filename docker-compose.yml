services:
  mihomo-manager:
    build:
      context: .
    container_name: mihomo-manager-webui
    restart: unless-stopped
    init: true
    pid: host
    privileged: true
    ports:
      - "${MIHOMO_MANAGER_BIND:-127.0.0.1}:5178:5178"
    environment:
      NODE_ENV: production
      LISTEN_HOST: 0.0.0.0
      PORT: 5178
      DATA_DIR: /app/data
      WEBUI_USERNAME: ${WEBUI_USERNAME:-admin}
      WEBUI_PASSWORD: ${WEBUI_PASSWORD:?Set WEBUI_PASSWORD}
      WEBUI_SESSION_SECRET: ${WEBUI_SESSION_SECRET:?Set WEBUI_SESSION_SECRET}
      WEBUI_COOKIE_SECURE: ${WEBUI_COOKIE_SECURE:-false}
      AUTH_ALLOW_REGISTRATION: ${AUTH_ALLOW_REGISTRATION:-false}
      SESSION_TTL_HOURS: ${SESSION_TTL_HOURS:-24}
      MIHOMO_LOCAL_RUNNER: ${MIHOMO_LOCAL_RUNNER:-nsenter}
      MIHOMO_MANAGER_HOST_PROJECT_DIR: ${MIHOMO_MANAGER_HOST_PROJECT_DIR:-/data/mihomo-manager-webui}
    volumes:
      - ./data:/app/data
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:5178/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
