services:
  mihomo-manager:
    environment:
      MIHOMO_KEY_FILE: /run/secrets/mihomo_ssh_key
    volumes:
      - ${MIHOMO_KEY_PATH:?Set MIHOMO_KEY_PATH for remote key mode}:/run/secrets/mihomo_ssh_key:ro
